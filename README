setup python virtual environment
run:
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

To start run:

python3 app.py

To test:
cd TestNodes

In multiple terminals one for each python file run:
python3 sin.py
python3 turtle_node.py
python3 turtle_kill_switch.py

Add more topics in the ros_topics.py file, should auto load into webpage
